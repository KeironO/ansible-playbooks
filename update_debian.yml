---
- name: Update Debian-based VMs
  hosts: all
  become: yes
  become_method: sudo  
  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Upgrade all packages
      apt:
        upgrade: dist